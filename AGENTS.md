# SHINKAI Agent Rules

このファイルはリポジトリ全体に適用される恒久ルールです。すべてのCodexスレッドとサブエージェントは、作業開始前に `docs/DESIGN.md` とこのファイルを読んでください。

## 必須の役割分担

1. ゲーム仕様、技術設計、データ設計、受け入れ基準、仕様変更は、thinking=`ultra` のCodex担当（Ultra）が決定する。
2. Ultraは設計書と設計レビューのみを担当し、ソースコード、テストコード、ビルド設定などの実装コードを作成・変更しない。
3. コーディング実装は、Ultraとは別に起動した `gpt-5.6-luna`、thinking=`max` のCodexスレッドまたはサブエージェント（Luna Max）だけが担当する。
4. Luna Maxは実装前に最新の `docs/DESIGN.md` を読み、仕様と受け入れ基準を遵守する。
5. Luna Maxは未記載のゲーム仕様を独断で追加・変更しない。設計判断が必要な場合はその箇所で止め、Ultraへ確認または設計書更新を依頼する。
6. 既存仕様を変えない内部的なバグ修正、リファクタリング、テスト追加はLuna Maxの裁量で行える。
7. 新しいセッション、スレッド、サブエージェントでも、この役割分担を必ず継承する。

## 優先順位

設計書とこのファイルが食い違う場合は、最新のUltra承認済み `docs/DESIGN.md` を正とする。ユーザーの最新の明示的な指示は常に最優先とする。
