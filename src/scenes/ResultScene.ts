import Phaser from "phaser";

/** Reserved scene shell for the result-screen roadmap stage. */
export class ResultScene extends Phaser.Scene {
  public constructor() {
    super("ResultScene");
  }
}
